import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { FormGroup } from './index';

<Meta title="Components/Form Group" component={FormGroup} />

# Form Group

This component is used in by all fields to allow consistency.

<Canvas>
  <Story name="Default">
    <FormGroup
      label="Label"
      helper="Helper"
      isRequired
    >
      -- input here --
    </FormGroup>
  </Story>
</Canvas>

<Canvas>
  <Story name="Error">
    <FormGroup
      label="Label"
      helper="Helper"
      errorMessage="Error message"
      showError
    >
      -- input here --
    </FormGroup>
  </Story>
</Canvas>

## Troubleshooting

- Helper will not be shown when the error is displayed (when `showError` is `true`). This comes from the Chakra UI FormControl components.
