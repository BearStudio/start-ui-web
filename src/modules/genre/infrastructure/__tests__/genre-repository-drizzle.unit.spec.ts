import { describe, it } from 'vitest';

describe('GenreRepositoryDrizzle integration', () => {
  // oxlint-disable-next-line
  // TODO(SPA-38): wire PGlite repository integration tests for search, pagination, and LIKE escaping.
  it.skip('covers search pagination and escaped LIKE behavior with PGlite', () => {});
});
