import { debugStyles } from '@/lib/debug'
import { Box, Stack } from '@chakra-ui/react';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="Utils/debug" />

# Debug components

```jsx
import { debugStyles } from '@/lib/debug'

// ...

<MyComponent {...debugStyles()}>
  // ...
</MyComponent>

```

## Default

<Canvas>
  <Story name="Default">
    <Box h="32" {...debugStyles()} />
  </Story>
</Canvas>


## With custom label and color and background

<Canvas>
  <Story name="With custom label and color">
    <Box
      h="32"
      {...debugStyles({
        label: 'MyComponent',
        colorScheme: 'brand',
        hideBackground: false
      })}
    />
  </Story>
</Canvas>


## Without label

<Canvas>
  <Story name="Without label">
    <Box h="32" {...debugStyles({ hideLabel: true })} />
  </Story>
</Canvas>

## Complex usage

<Canvas height="120px">
  <Story name="Real case usage">
    <Box p={4} {...debugStyles({ colorScheme: 'red', label: 'Container' })}>
      <Stack {...debugStyles({ colorScheme: 'cyan', label: 'List', hideBackground: false })}>
        <Box h="8" {...debugStyles({ label: 'ListItem 1', hideBackground: false })} />
        <Box h="8" w="90%" {...debugStyles({ label: 'ListItem 2', hideBackground: false })} />
        <Box h="8" w="50%" {...debugStyles({ label: 'ListItem 3', hideBackground: false })} />
        <Box h="8" w="60%" {...debugStyles({ label: 'ListItem 4', hideBackground: false })} />
      </Stack>
    </Box>
  </Story>
</Canvas>
