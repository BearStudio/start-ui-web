pre-commit:
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,jsx,tsx,css,scss}"
      run: pnpm oxfmt {staged_files}
      stage_fixed: true

pre-push:
  commands:
    check:
      run: pnpm lint
